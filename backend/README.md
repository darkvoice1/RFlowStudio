# Backend Scaffold

The backend uses FastAPI as the main service layer.

## Planned Responsibilities

- API routing
- task orchestration
- dataset metadata management
- integration with the R execution layer

## Install

```powershell
python -m venv ..\.venv
..\.venv\Scripts\Activate.ps1
Copy-Item .env.example .env
pip install -e .
```
